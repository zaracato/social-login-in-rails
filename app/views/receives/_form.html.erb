<%= simple_form_for(@receife, html:{class:'form-horizontal'}) do |f| %>
  <% if @receife.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@receife.errors.count, "error") %> prohibited this receife from being saved:</h2>

      <ul>
      <% @receife.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="panel">
    <div class="panel-heading">
      <span class="panel-title"></span>
    </div>
          
    <div class="panel-body">
      <%= render "receives/fields", f:f %>
    </div>
</div>
<div class="form-group">
  <div class="col-sm-8">
    <%= f.submit t('actions.save'), class:'btn btn-success' %>
  </div>
</div>
<% end %>