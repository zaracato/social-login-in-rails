<%= simple_form_for(@pay, html:{class:'form-horizontal'}) do |f| %>
  <% if @pay.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pay.errors.count, "error") %> prohibited this pay from being saved:</h2>

      <ul>
      <% @pay.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="panel">
    <div class="panel-heading">
      <span class="panel-title"></span>
    </div>
          
    <div class="panel-body">
      <%= render "pays/fields", f:f %>
    </div>
</div>
<div class="form-group">
  <div class="col-sm-8">
    <%= f.submit t('actions.save'), class:'btn btn-success' %>
  </div>
</div>
<% end %>