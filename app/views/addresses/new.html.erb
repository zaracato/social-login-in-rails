<%= content_for :title_link do %> 
  <%= link_to t('activerecord.models.addresses') ,addresses_path %> 
<% end %> 

<%= content_for :page_link do %> 
  <%= link_to t('actions.new') ,new_address_path %> 
<% end %> 


<%= render 'form' %>

<%= link_to t('actions.back'), addresses_path, class:'btn btn-default' %>